# meet-insights environment variables
# Set these at runtime (Docker -e or .env file)

# Required for live Gemini analysis
# Leave blank to run in stub mode (returns placeholder data)
GEMINI_API_KEY=
