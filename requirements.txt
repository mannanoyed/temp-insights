# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# Environment
python-dotenv>=1.0.0

# Gemini (both SDK variants for compatibility)
google-genai>=0.8.0
google-generativeai>=0.8.0

# HTTP client (for health checks etc)
requests>=2.32.0
